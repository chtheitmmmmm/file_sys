#: import RecieveFileView  components.RecieveFileView
#: import RecieveFileViewLayout  components.RecieveFileViewLayout

<RecieveFileView>:
    RecieveFileViewLayout:
        size_hint_y: (50 * (len(self.content.split('\n')) + 1) + 120) / root.size[1] if root.file else 1
        file: root.file

<RecieveFileViewLayout>:
