#: import MyFileView  components.MyFileView
#: import MyFileViewLayout  components.MyFileViewLayout


<MyFileView>:
    MyFileViewLayout:
        size_hint_y: (50 * (len(self.content.split('\n')) + 1) + 120) / root.size[1] if root.file else 1
        file: root.file

<MyFileViewLayout>